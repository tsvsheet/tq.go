version: 2

project_name: tq

dist: dist

# Shell completions and the man page ship in every archive and package. They
# are generated from the CLI's own command tree (the completion and man
# subcommands) into the gitignored var/ tree — never into dist/, which
# goreleaser requires empty after its own cleanup.
before:
  hooks:
    - mkdir -p var/extras/completions var/extras/man
    - sh -c "go run ./cmd/tq completion bash > var/extras/completions/tq.bash"
    - sh -c "go run ./cmd/tq completion zsh > var/extras/completions/tq.zsh"
    - sh -c "go run ./cmd/tq completion fish > var/extras/completions/tq.fish"
    - sh -c "go run ./cmd/tq man > var/extras/man/tq.1"

git:
  ignore_tags:
    - "saved/*"

builds:
  - id: "{{ .ProjectName }}"
    main: ./cmd/{{ .ProjectName }}
    binary: "{{ .ProjectName }}-{{ .Os }}-{{ .Arch }}"
    no_unique_dist_dir: true
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
      - arm
    goarm:
      - "7"
    ignore:
      - goos: windows
        goarch: arm64
      - goos: windows
        goarch: arm
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
    flags:
      - -buildvcs=false
      - -trimpath

archives:
  - id: default
    name_template: "{{ .ProjectName }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE*
      - README*
      - CHANGELOG*
      - src: var/extras/completions/*
        dst: completions
      - src: var/extras/man/*
        dst: man

nfpms:
  - id: packages
    package_name: tq
    vendor: tsvsheet
    homepage: https://tsvsheet.com
    maintainer: tsvsheet <noreply@tsvsheet.com>
    description: Query TSV with a pipeline of relational verbs — the tq query language CLI.
    license: MIT
    formats:
      - deb
      - rpm
    contents:
      - src: var/extras/completions/tq.bash
        dst: /usr/share/bash-completion/completions/tq
      - src: var/extras/completions/tq.zsh
        dst: /usr/share/zsh/site-functions/_tq
      - src: var/extras/completions/tq.fish
        dst: /usr/share/fish/vendor_completions.d/tq.fish
      - src: var/extras/man/tq.1
        dst: /usr/share/man/man1/tq.1

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  mode: keep-existing

snapshot:
  version_template: "0.0.0-SNAPSHOT-{{.ShortCommit}}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: Bug Fixes
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Others
      order: 999
